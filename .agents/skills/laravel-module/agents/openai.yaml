interface:
  display_name: "Laravel Module"
  short_description: "Repo-specific patterns and workflows for laravel-module"
  default_prompt: "Use the laravel-module repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true